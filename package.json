{
  "name": "quick-fridge-fix",
  "version": "1.0.0",
  "description": "Mobile-first recipe generator based on fridge ingredients",
  "main": "server/index.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite server/index.js",
    "dev": "nodemon --exec 'node --experimental-sqlite' server/index.js",
    "test": "NODE_ENV=test jest --testEnvironment node"
  },
  "dependencies": {

    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-session": "^1.18.0",
    "node-fetch": "^3.3.2",
    "openai": "^4.20.1",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
